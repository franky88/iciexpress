from django.shortcuts import render
# Create your views here.
# def classRosterList(request):
# 	classroster_list=ClassRoster.objects.all().order_by("-id")
# 	context = {
# 		"title": "class roster",
# 		"classrosterlist": classroster_list,
# 	}
# 	return render(request, "classrosters/classroster_list.html", context)